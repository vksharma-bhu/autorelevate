% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_autorelevate.R
\name{.robust_optim}
\alias{.robust_optim}
\title{Robust Optimization Helper}
\usage{
.robust_optim(obj_func, init_pars, label = "model")
}
\arguments{
\item{obj_func}{Objective function to minimize, taking a length-2
numeric vector.}

\item{init_pars}{Initial parameter vector (length 2).}

\item{label}{Short label used in the error message if all attempts fail.}
}
\value{
The best \code{optim()} result found (a list with at least
  \code{par}, \code{value}, \code{convergence}).
}
\description{
Wraps \code{\link[stats]{optim}} with a fallback chain: BFGS first,
then Nelder-Mead if BFGS fails or does not converge, then several
randomly perturbed Nelder-Mead restarts if both fail. Never lets a raw
optimizer error propagate to the user; instead raises a single clear
error only if every attempt fails.
}
\keyword{internal}
